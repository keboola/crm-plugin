# Keboola CRM — Claude Code plugin

Public distribution of the `keboola-crm` Claude Code plugin, auto-synced from
Keboolas CRM monorepo on each release.

## Install

```
/plugin marketplace add keboola/crm-plugin
/plugin install keboola-crm
```

_This repo is regenerated by CI; the first release sync replaces this file._
